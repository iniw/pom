image: nixos/latest
environment:
  NIX_CONFIG: "experimental-features = nix-command flakes"
sources:
  - https://git.sr.ht/~wini/pom
tasks:
  - check: |
      cd pom/
      nix flake check -L --keep-going
